# ravello-sdk test configuration
#
# Integration tests require access to the real Ravello service.  Fill in the
# details below to point to the it, then copy this file to "test.conf".

[integration]
username = <username>
password = <password>
url = <url or nothing for defaul>
image = <image ID of a Linux image for tests>
